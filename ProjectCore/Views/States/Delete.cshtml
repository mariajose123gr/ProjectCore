@model ProjectCore.DAL.Models.States

@{
    ViewData["Title"] = "Delete";
}

<h2>Delete</h2>

   class="badge  badge-danger">   Are you sure you want to delete this?</h3>
<div>
    <hr />
    <table class="table
           table-border">
        <tr>
            <td>
            @Html.DisplayNameFor(model => model.Name)
            </td>
            <td>
                @Html.DisplayFor(model => model.Active)
            </td>
            </tr>
        <tr>
            <td>

                @Html.DisplayNameFor(model => model.Name)
            <td>
            @Html.DisplayFor(model => model.Active)
            </td>
            </tr>
        </table>
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <button type="submit" class="btn btn-primary ">
            <i class="fa fas-save"></i>
        </button>
        @Html.ActionLink("", "Index", "States", null, new { @class = "btn btn-primary fas fa-undo-alt " })

    </form>
</div>
